<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:versionCode="1" android:versionName="0.1">

  <uses-feature android:glEsVersion="0x00020000" android:required="false"/>
  <!-- USB MIDI keyboard input: both marked not-required so the app still installs on devices
       without USB host mode or without the MIDI API - JUCE's own
       MidiInput::getAvailableDevices() just returns an empty list there, same as it already
       silently does for a phone with no keyboard attached. Class-compliant USB MIDI needs
       neither of these features to prompt for pairing/permission the way Bluetooth would.
       Same reasoning as the D110 Android port this was copied from. -->
  <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
  <uses-feature android:name="android.software.midi" android:required="false"/>

  <application android:label="@string/app_name" android:name="com.rmsl.juce.JuceApp"
               android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round"
               android:hardwareAccelerated="false">
    <activity android:name="com.rmsl.juce.JuceActivity"
              android:configChanges="keyboard|keyboardHidden|orientation|screenSize|navigation|smallestScreenSize|screenLayout|uiMode"
              android:launchMode="singleTask" android:hardwareAccelerated="true" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
